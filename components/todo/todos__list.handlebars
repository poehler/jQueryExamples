<div class="items">
<ul class="todo-list">
{{#each this}}
        <li class="todo-update-row hide" data-id="{{id}}">
			    <div class="column-1"><input name="todoName" type="text" value="{{todoName}}" class="todo-name-input-field"></div>
			    <div class="column-2"><select name="todoStatusCode" class="status-code-options">{{> status_code_options}}</select></div>
			    <div class="column-3 "><input name="targetCompletionDate" class="todo-date-field" type="text" placeholder="mm/dd/yyyy" value="{{formattedTargetCompletionDate}}"></div>
			    <div class="column-4 "><input name="actualCompletionDate" class="todo-date-field" type="text" placeholder="mm/dd/yyyy" value="{{formattedActualCompletionDate}}"></div>
			    <div class="column-5">
					<button class="todo-button btn-success todo-save-button todo-enabled">Save</button>
					<button class="todo-button btn-danger todo-reset-button">Reset</button>
			    </div>
        </li>
	    <li class="todo-list-row display-row" data-id="{{id}}">
	        <div class="column-1">{{todoName}}</div>
	        <div class="column-2">{{getStatus}}</div>
	        <div class="column-3">{{formattedTargetCompletionDate}}</div>
	        <div class="column-4">{{formattedActualCompletionDate}}&nbsp;</div>
	        <div class="column-5"> 
				<button class="btn-primary todo-button todo-edit-button">Edit</button>
				<button class="btn-danger todo-button todo-delete-button">Delete</button>
				<button class="btn-success todo-button todo-delete-confirmed-button hide">Yes</button>
				<button class="btn-danger todo-button todo-delete-not-confirmed-button hide">NO!</button>
	        </div>
	    </li>
	{{#if isEditing}}
    {{else}}
    {{/if}}
{{/each}}
</ul>
</div>